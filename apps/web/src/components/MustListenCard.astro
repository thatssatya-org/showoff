---
import MusicLinkCard from "./MusicLinkCard.astro";

interface Props {
  title: string;
  description: string;
  href: string;
}

const { title, description, href } = Astro.props;
---

<MusicLinkCard title={title} description={description} href={href} featured />
