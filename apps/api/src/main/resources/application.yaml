spring:
  application:
    name: portfolio-api
  jackson:
    default-property-inclusion: non_null
  mvc:
    problemdetails:
      enabled: true
springdoc:
  api-docs:
    path: /openapi.json
server:
  compression:
    enabled: true
    min-response-size: 1024
